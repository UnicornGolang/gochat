mysql: 
  url: "root:root@tcp(172.20.48.1:3306)/ginchat?charset=utf8mb4&parseTime=True&loc=Local"
redis:
  addr: "127.0.0.1:6379"
  password: foobared
  DB: 0
  poolSise: 30
  minIdleConns: 30

timeout:
  taskDelay: 3      # 系统启动后多久开始定时任务
  taskIdle: 30      # 每隔多久重复执行一次定时任务
  heatbeatMaxTime: 30000  # 最大心跳时间，超过此时间未收到心跳就下线
  redisOnlineTime: 4 # 缓存的在线用户时长

server:
  port: ":8080"
  udp: 3001

